(function(){
/* EZCut 前端主流程：Tauri桥接、状态管理、实时预览与批量导出。 */
/* 说明：此文件为紧凑写法，注释以模块级为主。 */
/* Tauri API 兼容层：兼容不同注入方式。 */
const T=window.__TAURI__||{},I=window.__TAURI_INTERNALS__||{};
const invoke=I.invoke||T.core?.invoke||T.tauri?.invoke||T.invoke||null;
const listen=T.event?.listen||T.listen||null;
const convert=I.convertFileSrc||T.core?.convertFileSrc||T.tauri?.convertFileSrc||T.convertFileSrc||((p)=>p);
/* 多语言文案表。 */
const tr={en:{appTitle:'EZCut',appSubtitle:'Batch image editor',previewEyebrow:'Preview',previewTitle:'Image Preview',addImages:'Add Images',dropHere:'Drop images here',dropSupport:'PNG, JPG, WEBP, TIFF, BMP, GIF',noImageSelected:'No image selected',libraryEyebrow:'Library',imagesTitle:'Images',noImagesYet:'No images yet.',settingsEyebrow:'Settings',outputSettings:'Output Settings',resize:'Resize',crop:'Crop',textWatermark:'Text Watermark',overlayImage:'Overlay Image',filename:'Filename',export:'Export',width:'Width',height:'Height',mode:'Mode',fit:'Fit',fill:'Fill',stretch:'Stretch',enableThis:'Enable this section',text:'Text',size:'Size',opacity:'Opacity',margin:'Margin',color:'Color',position:'Position',topLeft:'Top Left',topCenter:'Top Center',topRight:'Top Right',centerLeft:'Center Left',center:'Center',centerRight:'Center Right',bottomLeft:'Bottom Left',bottomCenter:'Bottom Center',bottomRight:'Bottom Right',choose:'Choose',noOverlaySelected:'No overlay selected',scalePercent:'Scale %',template:'Template',format:'Format',keepOriginal:'Keep original',useRegexRename:'Use regex rename',pattern:'Pattern',replace:'Replace',flags:'Flags',chooseSaveFolder:'Choose Save Folder',defaultSaveFolder:'Default: save next to each source image',defaultSaveFolderRuntime:'Default: saved next to each source image',clearAfterExport:'Clear image list after export',ready:'Ready',remove:'Remove',loadedCount:'Loaded {count} image(s)',overlaySelected:'Overlay selected',customSaveFolderUpdated:'Custom save folder updated',customSaveFolder:'Custom save folder: {path}',processing:'Processing...',tauriUnavailable:'Tauri API not available',addAtLeastOne:'Please add at least one image',doneSummary:'Done: {ok} exported, {fail} failed',processingFailed:'Processing failed: {error}',droppedCount:'Dropped {count} image(s)',remainingCount:'Remaining {count} image(s)',saved:'Saved: {path}',failed:'Failed: {path} - {message}',selectedMeta:'{name} · {width} x {height} · {size}',placeholderLeaveEmpty:'Leave empty',placeholderOriginalWidth:'Original width',placeholderOriginalHeight:'Original height',placeholderWatermark:'Watermark text',placeholderRegex:'^IMG_',placeholderReplace:'product_'},'zh-CN':{appTitle:'EZCut',appSubtitle:'批量图片编辑器',previewEyebrow:'预览',previewTitle:'图片预览',addImages:'添加图片',dropHere:'将图片拖到这里',dropSupport:'支持 PNG、JPG、WEBP、TIFF、BMP、GIF',noImageSelected:'未选择图片',libraryEyebrow:'列表',imagesTitle:'图片列表',noImagesYet:'还没有图片。',settingsEyebrow:'设置',outputSettings:'输出设置',resize:'修改尺寸',crop:'裁剪',textWatermark:'文字水印',overlayImage:'图层覆盖',filename:'文件名',export:'导出',width:'宽度',height:'高度',mode:'模式',fit:'等比适配',fill:'铺满裁切',stretch:'强制拉伸',enableThis:'启用此分组',text:'文字',size:'大小',opacity:'透明度',margin:'边距',color:'颜色',position:'位置',topLeft:'左上',topCenter:'上中',topRight:'右上',centerLeft:'左中',center:'居中',centerRight:'右中',bottomLeft:'左下',bottomCenter:'下中',bottomRight:'右下',choose:'选择',noOverlaySelected:'未选择覆盖图层',scalePercent:'缩放 %',template:'模板',format:'格式',keepOriginal:'保持原格式',useRegexRename:'启用正则改名',pattern:'正则',replace:'替换',flags:'标志',chooseSaveFolder:'选择保存目录',defaultSaveFolder:'默认保存到每张原图所在目录',defaultSaveFolderRuntime:'默认保存到每张原图所在目录',clearAfterExport:'导出后清空图片列表',ready:'就绪',remove:'移除',loadedCount:'已载入 {count} 张图片',overlaySelected:'已选择覆盖图层',customSaveFolderUpdated:'已更新自定义保存目录',customSaveFolder:'自定义保存目录：{path}',processing:'正在处理...',tauriUnavailable:'Tauri 接口不可用',addAtLeastOne:'请至少添加一张图片',doneSummary:'处理完成：成功 {ok} 张，失败 {fail} 张',processingFailed:'处理失败：{error}',droppedCount:'已拖入 {count} 张图片',remainingCount:'剩余 {count} 张图片',saved:'已保存：{path}',failed:'失败：{path} - {message}',selectedMeta:'{name} · {width} x {height} · {size}',placeholderLeaveEmpty:'留空则不限制',placeholderOriginalWidth:'原图宽度',placeholderOriginalHeight:'原图高度',placeholderWatermark:'输入水印文字',placeholderRegex:'^IMG_',placeholderReplace:'product_'}};
/* 运行期状态：集中管理文件、选中项与预览交互。 */
const s={files:[],selectedId:null,overlayFile:null,outputDirectory:'',platform:'windows',locale:'en',isDragOverlayVisible:false,previewCache:{},previewToken:0,previewImage:null,previewCropRect:null,previewWatermark:null,interaction:null,watermarkDraft:null};
const e={windowShell:document.querySelector('.window-shell'),leftColumn:document.getElementById('left-column'),fileInput:document.getElementById('file-input'),imageList:document.getElementById('image-list'),imageCount:document.getElementById('image-count'),previewCanvas:document.getElementById('preview-canvas'),previewMeta:document.getElementById('preview-meta'),pickFilesButton:document.getElementById('pick-files-button'),pickOverlayButton:document.getElementById('pick-overlay-button'),overlaySummary:document.getElementById('overlay-summary'),pickOutputButton:document.getElementById('pick-output-button'),outputDirectory:document.getElementById('output-directory'),processButton:document.getElementById('process-button'),clearAfterExport:document.getElementById('clear-after-export'),statusBox:document.getElementById('status-box'),resultList:document.getElementById('result-list'),dropOverlay:document.getElementById('drop-overlay'),languagePicker:document.getElementById('language-picker')};
let timer=null;
const n=(v,min,max)=>Math.min(max,Math.max(min,v));
const loc=(raw)=>{if(!raw)return'en';const v=raw.toLowerCase();if(v.startsWith('zh-cn')||v.startsWith('zh-sg'))return'zh-CN';if(v.startsWith('zh-tw')||v.startsWith('zh-hk')||v.startsWith('zh-mo'))return'zh-TW';if(v.startsWith('ja'))return'ja';if(v.startsWith('ko'))return'ko';if(v.startsWith('zh'))return'zh-CN';return'en';};
const tt=(k,vars={})=>((tr[s.locale]||tr.en)[k]||tr.en[k]||k).replace(/\{(\w+)\}/g,(_,t)=>String(vars[t]??''));
const num=(id)=>{const v=document.getElementById(id).value.trim();return v===''?null:Number(v);};
const val=(id)=>document.getElementById(id).value;
const chk=(id)=>document.getElementById(id).checked;
const rgb=(hex)=>{const h=hex.replace('#','');const v=(h.length===3?h.split('').map((p)=>p+p).join(''):h);const i=parseInt(v,16);return{r:(i>>16)&255,g:(i>>8)&255,b:i&255};};
const sel=()=>s.files.find((f)=>f.id===s.selectedId)||null;
const setStatus=(text,tone)=>{e.statusBox.textContent=text;e.statusBox.className=`info-box${tone?` ${tone}`:''}`;};
const cmd=async(c,a)=>{if(!invoke)throw new Error(tt('tauriUnavailable'));return invoke(c,a);};
const applyTranslations=()=>{document.documentElement.lang=s.locale;document.querySelectorAll('[data-i18n]').forEach((node)=>{node.textContent=tt(node.dataset.i18n);});document.getElementById('resize-width').placeholder=tt('placeholderLeaveEmpty');document.getElementById('resize-height').placeholder=tt('placeholderLeaveEmpty');document.getElementById('crop-width').placeholder=tt('placeholderOriginalWidth');document.getElementById('crop-height').placeholder=tt('placeholderOriginalHeight');document.getElementById('watermark-text').placeholder=tt('placeholderWatermark');document.getElementById('regex-pattern').placeholder=tt('placeholderRegex');document.getElementById('regex-replacement').placeholder=tt('placeholderReplace');e.pickFilesButton.title=tt('addImages');e.pickFilesButton.setAttribute('aria-label',tt('addImages'));};
const applyPlatform=()=>{document.body.classList.remove('platform-windows','platform-macos','platform-linux');document.body.classList.add(`platform-${s.platform}`);};
const renderOverlay=()=>e.dropOverlay.classList.toggle('is-visible',s.isDragOverlayVisible);
const renderLeft=()=>{e.windowShell.classList.toggle('has-files',s.files.length>0);e.leftColumn.classList.remove('is-hidden');};
const bytes=(size)=>{if(!size)return'0 B';const u=['B','KB','MB','GB'];let v=size,i=0;while(v>=1024&&i<u.length-1){v/=1024;i++;}return `${v.toFixed(v>=100||i===0?0:1)} ${u[i]}`;};
const pos=(v)=>{const p=String(v||'bottom-right').split('-');return{vertical:p[0]||'bottom',horizontal:p[1]||'right'};};
const anchor=(p,w,h,m)=>{const a=pos(p);return{x:a.horizontal==='left'?m:a.horizontal==='right'?w-m:w/2,y:a.vertical==='top'?m:a.vertical==='bottom'?h-m:h/2,align:a.horizontal==='left'?'left':a.horizontal==='right'?'right':'center',baseline:a.vertical==='top'?'top':a.vertical==='bottom'?'bottom':'middle'};};
const settings=()=>({crop:{enabled:chk('crop-enabled'),x:num('crop-x')||0,y:num('crop-y')||0,width:num('crop-width'),height:num('crop-height')},resize:{enabled:chk('resize-enabled'),width:num('resize-width'),height:num('resize-height'),mode:val('resize-mode')},watermark:{enabled:chk('watermark-enabled'),text:val('watermark-text').trim(),fontSize:num('watermark-font-size')||30,opacity:num('watermark-opacity')||0.35,margin:num('watermark-margin')||20,color:val('watermark-color'),position:val('watermark-position')},overlay:{enabled:chk('overlay-enabled'),scalePercent:num('overlay-scale')||25,opacity:num('overlay-opacity')||0.75,margin:num('overlay-margin')||20,position:val('overlay-position')}});
const loadImage=(src)=>new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=reject;img.src=src;});
const previewData=async(path)=>{if(s.previewCache[path])return s.previewCache[path];try{const p=await cmd('get_preview_data',{path});s.previewCache[path]=p.data_url;}catch(_){s.previewCache[path]=convert(path);}return s.previewCache[path];};
const ensureCropDefaults=(img)=>{if(!img||!chk('crop-enabled'))return;if(!val('crop-width'))document.getElementById('crop-width').value=String(img.naturalWidth);if(!val('crop-height'))document.getElementById('crop-height').value=String(img.naturalHeight);};
const cropRect=(img)=>{const c=settings().crop;if(!c.enabled||!img)return null;const w=n(c.width||img.naturalWidth,1,img.naturalWidth),h=n(c.height||img.naturalHeight,1,img.naturalHeight);return{x:n(c.x,0,img.naturalWidth-w),y:n(c.y,0,img.naturalHeight-h),width:w,height:h};};
const syncCrop=(r)=>{document.getElementById('crop-x').value=String(Math.round(r.x));document.getElementById('crop-y').value=String(Math.round(r.y));document.getElementById('crop-width').value=String(Math.round(r.width));document.getElementById('crop-height').value=String(Math.round(r.height));};
const canvasPoint=(event)=>{const rect=e.previewCanvas.getBoundingClientRect();return{x:(event.clientX-rect.left)*(e.previewCanvas.width/rect.width),y:(event.clientY-rect.top)*(e.previewCanvas.height/rect.height)};};
const watermarkModel=(ctx,w,h,wm)=>{if(!wm.enabled||!wm.text)return null;let x,y,fontSize;if(s.watermarkDraft){x=s.watermarkDraft.xRatio*w;y=s.watermarkDraft.yRatio*h;fontSize=Math.max(8,s.watermarkDraft.fontRatio*w);}else{fontSize=wm.fontSize;ctx.font=`600 ${fontSize}px -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`;const measured=ctx.measureText(wm.text).width;const a=anchor(wm.position,w,h,wm.margin);x=a.align==='left'?a.x:a.align==='right'?a.x-measured:a.x-measured/2;y=a.baseline==='top'?a.y:a.baseline==='bottom'?a.y-fontSize:a.y-fontSize/2;}ctx.font=`600 ${fontSize}px -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`;const measured=Math.max(1,ctx.measureText(wm.text).width);return{x:n(x,0,Math.max(0,w-measured)),y:n(y,0,Math.max(0,h-fontSize)),width:measured,height:fontSize,fontSize,text:wm.text};};
const drawCrop=(ctx,r,w,h)=>{ctx.save();ctx.fillStyle='rgba(18, 24, 33, 0.35)';ctx.beginPath();ctx.rect(0,0,w,h);ctx.rect(r.x,r.y,r.width,r.height);ctx.fill('evenodd');ctx.strokeStyle='rgba(37, 116, 255, 0.95)';ctx.lineWidth=Math.max(2,w/500);ctx.strokeRect(r.x,r.y,r.width,r.height);const size=Math.max(8,w/60);[[r.x,r.y],[r.x+r.width,r.y],[r.x,r.y+r.height],[r.x+r.width,r.y+r.height]].forEach(([x,y])=>{ctx.fillStyle='#fff';ctx.strokeStyle='rgba(37, 116, 255, 0.95)';ctx.fillRect(x-size/2,y-size/2,size,size);ctx.strokeRect(x-size/2,y-size/2,size,size);});ctx.restore();};
const drawWatermark=(ctx,m,wm)=>{const c=rgb(wm.color);ctx.save();ctx.font=`600 ${m.fontSize}px -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`;ctx.textAlign='left';ctx.textBaseline='top';ctx.fillStyle=`rgba(${c.r}, ${c.g}, ${c.b}, ${wm.opacity})`;ctx.fillText(m.text,m.x,m.y);ctx.strokeStyle='rgba(37, 116, 255, 0.95)';ctx.lineWidth=Math.max(1,m.fontSize/18);ctx.strokeRect(m.x-4,m.y-4,m.width+8,m.height+8);const hs=Math.max(8,m.fontSize/3);ctx.fillStyle='#fff';ctx.fillRect(m.x+m.width-hs/2,m.y+m.height-hs/2,hs,hs);ctx.strokeRect(m.x+m.width-hs/2,m.y+m.height-hs/2,hs,hs);ctx.restore();};
const schedule=(delay=80)=>{clearTimeout(timer);timer=setTimeout(()=>{void renderPreview();},delay);};
async function renderPreview(){const token=++s.previewToken,file=sel(),canvas=e.previewCanvas,ctx=canvas.getContext('2d');s.previewImage=null;s.previewCropRect=null;s.previewWatermark=null;if(!file){canvas.width=1;canvas.height=1;ctx.clearRect(0,0,1,1);e.previewMeta.textContent=tt('noImageSelected');return;}e.previewMeta.textContent=tt('selectedMeta',{name:file.name,width:file.width,height:file.height,size:bytes(file.size_bytes)});try{const src=await previewData(file.path),img=await loadImage(src);if(token!==s.previewToken||sel()?.path!==file.path)return;s.previewImage=img;ensureCropDefaults(img);canvas.width=img.naturalWidth;canvas.height=img.naturalHeight;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(img,0,0,canvas.width,canvas.height);const st=settings();if(st.overlay.enabled&&s.overlayFile){const overlaySrc=await previewData(s.overlayFile.path),overlayImg=await loadImage(overlaySrc);if(token!==s.previewToken||sel()?.path!==file.path)return;const ow=Math.max(1,Math.round(canvas.width*(st.overlay.scalePercent/100))),oh=Math.max(1,Math.round(overlayImg.naturalHeight*(ow/overlayImg.naturalWidth))),p=pos(st.overlay.position),ox=p.horizontal==='left'?st.overlay.margin:p.horizontal==='right'?canvas.width-ow-st.overlay.margin:Math.round((canvas.width-ow)/2),oy=p.vertical==='top'?st.overlay.margin:p.vertical==='bottom'?canvas.height-oh-st.overlay.margin:Math.round((canvas.height-oh)/2);ctx.save();ctx.globalAlpha=st.overlay.opacity;ctx.drawImage(overlayImg,ox,oy,ow,oh);ctx.restore();}if(st.watermark.enabled&&st.watermark.text){const wm=watermarkModel(ctx,canvas.width,canvas.height,st.watermark);s.previewWatermark=wm;drawWatermark(ctx,wm,st.watermark);}if(st.crop.enabled){const cr=cropRect(img);s.previewCropRect=cr;drawCrop(ctx,cr,canvas.width,canvas.height);}}catch(_){canvas.width=1;canvas.height=1;ctx.clearRect(0,0,1,1);}}
const renderFiles=()=>{renderLeft();e.imageCount.textContent=String(s.files.length);e.imageList.innerHTML='';if(s.files.length===0){s.selectedId=null;e.imageList.classList.add('empty-state');e.imageList.innerHTML=`<p>${tt('noImagesYet')}</p>`;schedule(0);return;}if(!sel())s.selectedId=s.files[0].id;e.imageList.classList.remove('empty-state');const frag=document.createDocumentFragment();s.files.forEach((file)=>{const item=document.createElement('article');item.className=`image-item${file.id===s.selectedId?' selected':''}`;item.dataset.id=file.id;item.innerHTML=`<div class="image-item-head"><span class="image-name" title="${file.name}">${file.name}</span><button class="remove-button" type="button" data-id="${file.id}">${tt('remove')}</button></div><div class="image-item-meta"><span>${file.width} x ${file.height}</span><span>${bytes(file.size_bytes)}</span></div><div class="image-path" title="${file.path}">${file.path}</div>`;frag.appendChild(item);});e.imageList.appendChild(frag);schedule(0);};
const renderOverlayFile=()=>{if(!s.overlayFile){e.overlaySummary.textContent=tt('noOverlaySelected');return;}e.overlaySummary.textContent=`${s.overlayFile.name} · ${s.overlayFile.width} x ${s.overlayFile.height}`;schedule(0);};
const mergeFiles=(files)=>{const known=new Set(s.files.map((i)=>i.path));files.forEach((file)=>{if(!known.has(file.path)){s.files.push(file);known.add(file.path);}});if(!s.selectedId&&s.files.length>0)s.selectedId=s.files[0].id;renderFiles();};
const clearImages=()=>{s.files=[];s.selectedId=null;s.previewCropRect=null;s.previewWatermark=null;s.watermarkDraft=null;renderFiles();};
async function buildWatermarkData(){const wm=settings().watermark;if(!wm.enabled||!wm.text)return null;const canvas=document.createElement('canvas');canvas.width=1600;canvas.height=1000;const ctx=canvas.getContext('2d'),c=rgb(wm.color);if(s.watermarkDraft){const fontSize=Math.max(8,s.watermarkDraft.fontRatio*canvas.width);ctx.font=`600 ${fontSize}px -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`;ctx.textAlign='left';ctx.textBaseline='top';ctx.fillStyle=`rgba(${c.r}, ${c.g}, ${c.b}, ${wm.opacity})`;ctx.fillText(wm.text,s.watermarkDraft.xRatio*canvas.width,s.watermarkDraft.yRatio*canvas.height);}else{const a=anchor(wm.position,canvas.width,canvas.height,wm.margin);ctx.font=`600 ${wm.fontSize}px -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`;ctx.textAlign=a.align;ctx.textBaseline=a.baseline;ctx.fillStyle=`rgba(${c.r}, ${c.g}, ${c.b}, ${wm.opacity})`;ctx.fillText(wm.text,a.x,a.y);}return canvas.toDataURL('image/png').replace(/^data:image\/png;base64,/,'');}
async function pickFiles(){try{if(invoke){mergeFiles(await cmd('pick_files'));setStatus(tt('loadedCount',{count:s.files.length}),'success');}else e.fileInput.click();}catch(error){setStatus(tt('processingFailed',{error:String(error)}),'error');}}
async function pickOverlay(){try{const file=await cmd('pick_overlay');if(!file)return;s.overlayFile=file;renderOverlayFile();setStatus(tt('overlaySelected'),'success');}catch(error){setStatus(tt('processingFailed',{error:String(error)}),'error');}}
async function pickOutputDirectory(){try{const directory=await cmd('pick_output_directory',{current_dir:s.outputDirectory||null});if(!directory)return;s.outputDirectory=directory;e.outputDirectory.textContent=tt('customSaveFolder',{path:directory});setStatus(tt('customSaveFolderUpdated'),'success');}catch(error){setStatus(tt('processingFailed',{error:String(error)}),'error');}}
const payload=(watermark_png_base64)=>({files:s.files,output_directory:s.outputDirectory||null,resize:{enabled:chk('resize-enabled'),width:num('resize-width'),height:num('resize-height'),mode:val('resize-mode')},crop:{enabled:chk('crop-enabled'),x:num('crop-x')||0,y:num('crop-y')||0,width:num('crop-width'),height:num('crop-height')},overlay:{enabled:chk('overlay-enabled'),path:s.overlayFile?s.overlayFile.path:null,scale_percent:num('overlay-scale')||25,opacity:num('overlay-opacity')||0.75,margin:num('overlay-margin')||20,position:val('overlay-position')},naming:{template:val('output-template')||'{name}_edit',output_format:val('output-format'),regex_enabled:chk('regex-enabled'),regex_pattern:val('regex-pattern'),regex_replacement:val('regex-replacement'),regex_flags:val('regex-flags')},watermark_png_base64});
const renderResults=(results,errors)=>{e.resultList.innerHTML='';const frag=document.createDocumentFragment();results.forEach((item)=>{const line=document.createElement('div');line.className='info-box result-item success';line.textContent=tt('saved',{path:item.output_path});frag.appendChild(line);});errors.forEach((item)=>{const line=document.createElement('div');line.className='info-box result-item error';line.textContent=tt('failed',{path:item.file_path,message:item.message});frag.appendChild(line);});e.resultList.appendChild(frag);};
/* 主导出流程：构建 payload 并调用后端批处理。 */
async function processImages(){if(s.files.length===0){setStatus(tt('addAtLeastOne'),'error');return;}e.processButton.disabled=true;e.resultList.innerHTML='';setStatus(tt('processing'));try{const result=await cmd('process_images',{payload:payload(await buildWatermarkData())});renderResults(result.results,result.errors);e.outputDirectory.textContent=s.outputDirectory?tt('customSaveFolder',{path:s.outputDirectory}):tt('defaultSaveFolderRuntime');setStatus(tt('doneSummary',{ok:result.results.length,fail:result.errors.length}),result.errors.length?'error':'success');if(e.clearAfterExport.checked&&result.results.length>0)clearImages();}catch(error){setStatus(tt('processingFailed',{error:String(error)}),'error');}finally{e.processButton.disabled=false;}}
/* 拖拽导入流程：路径交给后端做真实格式检测。 */
async function loadDroppedPaths(paths){if(!Array.isArray(paths)||paths.length===0)return;try{mergeFiles(await cmd('load_files_from_paths',{paths}));s.isDragOverlayVisible=false;renderOverlay();setStatus(tt('droppedCount',{count:paths.length}),'success');}catch(error){setStatus(tt('processingFailed',{error:String(error)}),'error');}}
async function loadPathsFromFileList(fileList){const paths=Array.from(fileList||[]).map((file)=>file.path).filter(Boolean);if(paths.length===0){setStatus(tt('processingFailed',{error:'Local file paths are unavailable in this runtime'}),'error');return;}await loadDroppedPaths(paths);}
const storeLocale=()=>{try{localStorage.setItem('ezcut-locale',s.locale);}catch(_){}};
const getStoredLocale=()=>{try{return localStorage.getItem('ezcut-locale');}catch(_){return null;}};
const applyLocale=(locale)=>{s.locale=loc(locale);e.languagePicker.value=s.locale;applyTranslations();renderFiles();renderOverlayFile();e.outputDirectory.textContent=s.outputDirectory?tt('customSaveFolder',{path:s.outputDirectory}):tt('defaultSaveFolder');setStatus(tt('ready'));};
async function init(){s.locale=loc(navigator.language);try{const info=invoke?await cmd('get_runtime_info'):null;if(info?.platform)s.platform=info.platform;if(info?.locale)s.locale=loc(info.locale);}catch(_){const ua=navigator.userAgent.toLowerCase();s.platform=ua.includes('mac')?'macos':ua.includes('linux')?'linux':'windows';}const stored=getStoredLocale();if(stored)s.locale=loc(stored);applyPlatform();applyLocale(s.locale);renderLeft();renderOverlay();}
const hitCropHandle=(pt,r)=>{if(!r)return null;const size=Math.max(10,e.previewCanvas.width/50),handles={nw:{x:r.x,y:r.y},ne:{x:r.x+r.width,y:r.y},sw:{x:r.x,y:r.y+r.height},se:{x:r.x+r.width,y:r.y+r.height}};return Object.entries(handles).find(([,h])=>Math.abs(pt.x-h.x)<=size&&Math.abs(pt.y-h.y)<=size)?.[0]||null;};
const hitWatermark=(pt,wm)=>{if(!wm)return null;const size=Math.max(10,wm.fontSize/2);if(Math.abs(pt.x-(wm.x+wm.width))<=size&&Math.abs(pt.y-(wm.y+wm.height))<=size)return'resize';if(pt.x>=wm.x-6&&pt.x<=wm.x+wm.width+6&&pt.y>=wm.y-6&&pt.y<=wm.y+wm.height+6)return'move';return null;};
const setWatermarkDraft=(x,y,fontSize,w,h)=>{const cw=e.previewCanvas.width,ch=e.previewCanvas.height;const px=n(x,0,Math.max(0,cw-w)),py=n(y,0,Math.max(0,ch-h));document.getElementById('watermark-font-size').value=String(Math.round(fontSize));s.watermarkDraft={xRatio:px/cw,yRatio:py/ch,fontRatio:fontSize/cw};};
function onPreviewDown(event){if(!s.previewImage)return;const pt=canvasPoint(event),cropHandle=chk('crop-enabled')?hitCropHandle(pt,s.previewCropRect):null;if(cropHandle){s.interaction={type:'crop-resize',handle:cropHandle,start:pt,rect:{...s.previewCropRect}};}else if(chk('crop-enabled')&&s.previewCropRect&&pt.x>=s.previewCropRect.x&&pt.x<=s.previewCropRect.x+s.previewCropRect.width&&pt.y>=s.previewCropRect.y&&pt.y<=s.previewCropRect.y+s.previewCropRect.height){s.interaction={type:'crop-move',start:pt,rect:{...s.previewCropRect}};}else{const wmMode=chk('watermark-enabled')?hitWatermark(pt,s.previewWatermark):null;if(wmMode)s.interaction={type:`watermark-${wmMode}`,start:pt,watermark:{...s.previewWatermark}};}if(s.interaction){e.previewCanvas.setPointerCapture(event.pointerId);event.preventDefault();}}
function onPreviewMove(event){if(!s.interaction||!s.previewImage)return;const pt=canvasPoint(event),dx=pt.x-s.interaction.start.x,dy=pt.y-s.interaction.start.y;if(s.interaction.type==='crop-move'){const r=s.interaction.rect;syncCrop({x:n(r.x+dx,0,s.previewImage.naturalWidth-r.width),y:n(r.y+dy,0,s.previewImage.naturalHeight-r.height),width:r.width,height:r.height});schedule(0);return;}if(s.interaction.type==='crop-resize'){const r=s.interaction.rect,minSize=Math.max(20,Math.min(s.previewImage.naturalWidth,s.previewImage.naturalHeight)/12);let x=r.x,y=r.y,w=r.width,h=r.height;if(s.interaction.handle.includes('w')){x=n(r.x+dx,0,r.x+r.width-minSize);w=r.width+(r.x-x);}if(s.interaction.handle.includes('e'))w=n(r.width+dx,minSize,s.previewImage.naturalWidth-r.x);if(s.interaction.handle.includes('n')){y=n(r.y+dy,0,r.y+r.height-minSize);h=r.height+(r.y-y);}if(s.interaction.handle.includes('s'))h=n(r.height+dy,minSize,s.previewImage.naturalHeight-r.y);syncCrop({x,y,width:w,height:h});schedule(0);return;}if(s.interaction.type==='watermark-move'){const wm=s.interaction.watermark;setWatermarkDraft(wm.x+dx,wm.y+dy,wm.fontSize,wm.width,wm.height);schedule(0);return;}if(s.interaction.type==='watermark-resize'){const wm=s.interaction.watermark,fontSize=n(wm.fontSize+(dx+dy)/2,8,e.previewCanvas.width*0.35),scale=fontSize/wm.fontSize;setWatermarkDraft(wm.x,wm.y,fontSize,wm.width*scale,wm.height*scale);schedule(0);}}
function onPreviewUp(event){if(!s.interaction)return;try{e.previewCanvas.releasePointerCapture(event.pointerId);}catch(_){}s.interaction=null;}
e.pickFilesButton.addEventListener('click',pickFiles);e.pickOverlayButton.addEventListener('click',pickOverlay);e.pickOutputButton.addEventListener('click',pickOutputDirectory);e.processButton.addEventListener('click',processImages);e.previewCanvas.addEventListener('pointerdown',onPreviewDown);e.previewCanvas.addEventListener('pointermove',onPreviewMove);e.previewCanvas.addEventListener('pointerup',onPreviewUp);e.previewCanvas.addEventListener('pointercancel',onPreviewUp);
e.languagePicker.addEventListener('change',(event)=>{applyLocale(event.target.value);storeLocale();});
e.fileInput.addEventListener('change',async(event)=>{await loadPathsFromFileList(event.target.files);event.target.value='';});
e.imageList.addEventListener('click',(event)=>{const removable=event.target.closest('.remove-button'),item=event.target.closest('.image-item');if(removable){const id=removable.dataset.id;s.files=s.files.filter((f)=>f.id!==id);if(s.selectedId===id)s.selectedId=s.files[0]?.id||null;renderFiles();setStatus(tt('remainingCount',{count:s.files.length}));return;}if(item){s.selectedId=item.dataset.id;s.watermarkDraft=null;renderFiles();}});
['resize-enabled','resize-width','resize-height','resize-mode','crop-enabled','crop-x','crop-y','crop-width','crop-height','watermark-enabled','watermark-text','watermark-font-size','watermark-opacity','watermark-margin','watermark-color','watermark-position','overlay-enabled','overlay-scale','overlay-opacity','overlay-margin','overlay-position'].forEach((id)=>{const node=document.getElementById(id);if(!node)return;node.addEventListener('input',()=>{if(id.startsWith('watermark-')&&id!=='watermark-opacity'&&id!=='watermark-color')s.watermarkDraft=null;schedule(0);});node.addEventListener('change',()=>{if(id==='crop-enabled'&&s.previewImage)ensureCropDefaults(s.previewImage);if(id.startsWith('watermark-')&&id!=='watermark-opacity'&&id!=='watermark-color')s.watermarkDraft=null;schedule(0);});});
document.addEventListener('dragenter',(event)=>{if(event.dataTransfer?.types?.includes('Files')){event.preventDefault();s.isDragOverlayVisible=true;renderOverlay();}});document.addEventListener('dragover',(event)=>{event.preventDefault();if(event.dataTransfer?.types?.includes('Files')){s.isDragOverlayVisible=true;renderOverlay();}});document.addEventListener('dragleave',(event)=>{if(event.relatedTarget===null){s.isDragOverlayVisible=false;renderOverlay();}});document.addEventListener('drop',async(event)=>{event.preventDefault();s.isDragOverlayVisible=false;renderOverlay();await loadPathsFromFileList(event.dataTransfer?.files);});
if(listen){listen('tauri://file-drop-hover',()=>{s.isDragOverlayVisible=true;renderOverlay();});listen('tauri://file-drop-cancelled',()=>{s.isDragOverlayVisible=false;renderOverlay();});listen('tauri://file-drop',async(event)=>{await loadDroppedPaths(event.payload);});}
init();
})();



